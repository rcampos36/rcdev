ADMIN_PASSWORD=change-me

# Required to save images and CMS content on Vercel (read-only filesystem).
# Vercel dashboard → Storage → Create Blob store → connect this project, then redeploy.
BLOB_READ_WRITE_TOKEN=

